# More model options

SmlLM3 weights a few GB. We should add more model options for users to choose from so they can use smaller models if they want to.

# Warn users about the download size

And also use a specific window and track the download progress.